import { CustomerAccountVo } from './customer-account-vo';

describe('CustomerAccountVo', () => {
  it('should create an instance', () => {
    expect(new CustomerAccountVo()).toBeTruthy();
  });
});
